local WindUI = loadstring(game:HttpGet("https://github.com/Footagesus/WindUI/releases/latest/download/main.lua"))()

local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local CollectionService = game:GetService("CollectionService")
local LocalPlayer = Players.LocalPlayer

local Knit = require(ReplicatedStorage.Packages.knit)

local AntiCheatController = Knit.GetController("AntiCheatController")
if AntiCheatController then
    pcall(function()
        AntiCheatController.KnitStart = function() end
        AntiCheatController.KnitInit = function() end
    end)
    pcall(function()
        local AntiCheatService = Knit.GetService("AntiCheatService")
        if AntiCheatService then
            AntiCheatService.IncidentWarning = { Connect = function() return { Disconnect = function() end } end }
            AntiCheatService.GetPendingWarning = function() return { await = function() return false, nil end } end
            AntiCheatService.SetCheckState = function() return { await = function() return true, { Success = true } end } end
            AntiCheatService.GetSettings = function() return { await = function() return true, { Authorized = false } end } end
        end
    end)
    pcall(function()
        local controllers = Knit.Controllers
        if controllers then
            controllers["AntiCheatController"] = nil
        end
    end)
end

local DataService = Knit.GetService("DataService")
local RelicsService = Knit.GetService("RelicsService")
local AuraService = Knit.GetService("AuraService")
local CuttersShopService = Knit.GetService("CuttersShopService")
local UpgradesService = Knit.GetService("UpgradesService")
local RebirtService = Knit.GetService("RebirtService")

local RelicsConfig = require(ReplicatedStorage.Shared.Configs.Relics)
local AurasConfig = require(ReplicatedStorage.Shared.Configs.Auras)

local StartCraftRemote = ReplicatedStorage.Packages._Index["acecateer_knit@1.7.2"].knit.Services.RelicsService.RF.StartCraft
local ClaimCraftResultRemote = ReplicatedStorage.Packages._Index["acecateer_knit@1.7.2"].knit.Services.RelicsService.RF.ClaimCraftResult
local SellRemote = ReplicatedStorage.Packages._Index["acecateer_knit@1.7.2"].knit.Services.DataService.RF.SellAllBackpackLoot
local ClickRequestedRemote = ReplicatedStorage.Packages._Index["acecateer_knit@1.7.2"].knit.Services.StrengthService.RE.ClickRequested

local rarityOrder = {
    "Singularity",
    "Omniversal",
    "Empyrean",
    "Primordial",
    "Ascendant",
    "Cosmic",
    "Transcendent",
    "Eternal",
    "Celestial",
    "Divine",
    "Godly",
    "Secret",
    "Mythic",
    "Legendary",
    "Epic",
    "Rare",
    "Uncommon",
    "Common"
}
local allRarities = {
    "Common",
    "Uncommon",
    "Rare",
    "Epic",
    "Legendary",
    "Mythic",
    "Secret",
    "Godly",
    "Divine",
    "Celestial",
    "Eternal",
    "Transcendent",
    "Cosmic",
    "Ascendant",
    "Primordial",
    "Empyrean",
    "Omniversal",
    "Singularity"
}
local upgradeDisplayNames = { "Mobility", "Capacity", "Agility", "Range" }
local displayToInternal = {
    ["Mobility"] = "Speed",
    ["Capacity"] = "Carry",
    ["Agility"] = "Attack Speed",
    ["Range"] = "Attack Range"
}

local relicNames = {}
local relicIdByName = {}
local relicCostById = {}
for _, item in ipairs(RelicsConfig.Items) do
    local name = item.DisplayName
    table.insert(relicNames, name)
    relicIdByName[name] = item.Id
    local recipe = RelicsConfig.RecipesById[item.Id]
    relicCostById[item.Id] = recipe and tonumber(recipe.EssenceCost) or math.huge
end
table.sort(relicNames)

local auraOrder = AurasConfig.Order or {}
local auraNames = {}
for _, name in ipairs(auraOrder) do
    table.insert(auraNames, name)
end
if #auraNames == 0 then
    for name in pairs(AurasConfig.Items) do
        table.insert(auraNames, name)
    end
    table.sort(auraNames)
end

local selectedRecipes = {}
local autoCraftEnabled = false
local autoEquipRelicEnabled = false
local autoCollectCraftedEnabled = false
local isCrafting = false
local Essence = 0
local isEquippingRelic = false

local autoBuyAuraEnabled = false
local autoEquipAuraEnabled = false
local auraState = nil
local isBusyAura = false

local autoBuyCutterEnabled = false
local autoEquipCutterEnabled = false
local cuttersData = {}
local ownedCutters = {}
local currentCutter = ""
local isBuyingCutter = false

local selectedUpgrades = { "Mobility" }
local autoUpgradesEnabled = false
local upgradePrices = { ["Speed"] = math.huge, ["Carry"] = math.huge, ["Attack Speed"] = math.huge, ["Attack Range"] = math.huge }
local upgradeMaxed = { ["Speed"] = false, ["Carry"] = false, ["Attack Speed"] = false, ["Attack Range"] = false }
local money = 0

local autoCollectLootEnabled = false
local selectedLootRarityMap = { ["Common"] = true }
local savedLootPos = nil
local backpackFull = false

local autoRebirthEnabled = false
local rebirthCooldown = 0

local autoClickEnabled = false

local function getHRP()
    local character = LocalPlayer.Character
    if not character then return nil end
    local hrp = character:FindFirstChild("HumanoidRootPart")
    if not hrp then hrp = character:WaitForChild("HumanoidRootPart", 2) end
    return hrp
end

local function returnToSavedPosition()
    if savedLootPos then
        local hrp = getHRP()
        if hrp then hrp.CFrame = CFrame.new(savedLootPos) end
    end
end

local Window = WindUI:CreateWindow({
    Title = "[X2] +1 Cut Grass Adventure",
    Icon = "bot",
    Author = "You",
    Folder = "AutoFarmHub",
    Size = UDim2.fromOffset(580, 480),
    MinSize = Vector2.new(500, 400),
    Theme = "Dark"
})

local TabChangelog = Window:Tab({ Title = "Changelog", Icon = "scroll" })
local TabRebirth = Window:Tab({ Title = "Rebirth", Icon = "refresh-cw" })
local TabRelics = Window:Tab({ Title = "Relics", Icon = "hammer" })
local TabAuras = Window:Tab({ Title = "Auras", Icon = "sparkles" })
local TabCutters = Window:Tab({ Title = "Cutters", Icon = "scissors" })
local TabUpgrades = Window:Tab({ Title = "Upgrades", Icon = "trending-up" })
local TabLoot = Window:Tab({ Title = "Loot", Icon = "gem" })
local TabStrength = Window:Tab({ Title = "Strength", Icon = "zap" })

TabChangelog:Paragraph({
    Title = "Changelog",
    Desc = "v1.0 - Initial release of combined Auto Farm Hub.\n\n- Auto Rebirth\n- Relic crafting & equip\n- Aura buying & equip\n- Cutter buying & equip\n- Upgrades auto purchase\n- Loot collection with rarity selection\n- Auto clicker for Strength\n\nAll features run independently and can be toggled on/off.",
    Color = "Green"
})

TabRebirth:Toggle({
    Title = "Auto Rebirth",
    Desc = "Automatically rebirths when rebirth is available, with a 3-second cooldown.",
    Value = false,
    Callback = function(state)
        autoRebirthEnabled = state
        rebirthCooldown = 0
    end
})

TabRelics:Dropdown({
    Title = "Relic Recipes",
    Desc = "Choose which relics to craft.",
    Values = relicNames,
    Value = {},
    Multi = true,
    AllowNone = true,
    Callback = function(selected)
        selectedRecipes = selected
    end
})
TabRelics:Toggle({
    Title = "Auto Craft",
    Desc = "Automatically crafts the selected relics every 0.5 seconds when enough Essence is available.",
    Value = false,
    Callback = function(state) autoCraftEnabled = state end
})
TabRelics:Toggle({
    Title = "Auto Equip Best Relic",
    Desc = "Automatically fills all available relic slots with your best owned relics.",
    Value = false,
    Callback = function(state) autoEquipRelicEnabled = state; isEquippingRelic = false end
})
TabRelics:Toggle({
    Title = "Auto Collect Crafted Relic",
    Desc = "Automatically collects completed relics every 1 second.",
    Value = false,
    Callback = function(state) autoCollectCraftedEnabled = state end
})

TabAuras:Toggle({
    Title = "Auto Buy Aura",
    Desc = "Automatically buys the cheapest aura you can afford every 0.5 seconds.",
    Value = false,
    Callback = function(state) autoBuyAuraEnabled = state end
})
TabAuras:Toggle({
    Title = "Auto Equip Best Aura",
    Desc = "Automatically equips your strongest owned aura every 0.1 seconds.",
    Value = false,
    Callback = function(state) autoEquipAuraEnabled = state end
})

TabCutters:Toggle({
    Title = "Auto Buy Cutter",
    Desc = "Automatically buys the cheapest cutter you can afford every 0.5 seconds.",
    Value = false,
    Callback = function(state) autoBuyCutterEnabled = state end
})
TabCutters:Toggle({
    Title = "Auto Equip Best Cutter",
    Desc = "Automatically equips your strongest owned cutter every 0.1 seconds.",
    Value = false,
    Callback = function(state) autoEquipCutterEnabled = state end
})

TabUpgrades:Dropdown({
    Title = "Upgrades",
    Desc = "Choose which upgrades to purchase: Mobility, Capacity, Agility, or Range.",
    Values = upgradeDisplayNames,
    Value = { "Mobility" },
    Multi = true,
    AllowNone = true,
    Callback = function(selected) selectedUpgrades = selected end
})
TabUpgrades:Toggle({
    Title = "Auto Buy",
    Desc = "Automatically purchases the selected upgrades every 0.1 seconds when affordable.",
    Value = false,
    Callback = function(state) autoUpgradesEnabled = state end
})

TabLoot:Dropdown({
    Title = "Loot Rarities",
    Desc = "Choose which loot rarities to collect.",
    Values = allRarities,
    Value = { "Common" },
    Multi = true,
    AllowNone = true,
    Callback = function(selected)
        selectedLootRarityMap = {}
        for _, r in ipairs(selected) do selectedLootRarityMap[r] = true end
    end
})
TabLoot:Toggle({
    Title = "Auto Collect",
    Desc = "Automatically collects selected loot and returns to your saved position when disabled.",
    Value = false,
    Callback = function(state)
        autoCollectLootEnabled = state
        if state then
            local hrp = getHRP()
            if hrp then savedLootPos = hrp.Position end
        else
            returnToSavedPosition()
        end
    end
})
TabLoot:Button({
    Title = "Sell All Now",
    Desc = "Instantly sells all collected loot.",
    Callback = function()
        pcall(function()
            SellRemote:InvokeServer()
            WindUI:Notify({ Title = "Loot", Content = "Sold all loot.", Duration = 2, Icon = "check" })
        end)
    end
})

TabStrength:Toggle({
    Title = "Auto Gain Strength",
    Desc = "Automatically performs strength clicks every 0.01 seconds to continuously gain Strength.",
    Value = false,
    Callback = function(state) autoClickEnabled = state end
})

local function refreshEssence()
    pcall(function()
        DataService:GetData("Essence"):andThen(function(v) if type(v) == "number" then Essence = v end end):catch(function() end)
    end)
    pcall(function()
        DataService:GetData("GrassEssence"):andThen(function(v) if type(v) == "number" then Essence = v end end):catch(function() end)
    end)
end
pcall(function()
    DataService.UpdateEssence:Connect(function(v) if type(v) == "number" then Essence = v end end)
end)
pcall(function()
    DataService.UpdateGrassEssence:Connect(function(v) if type(v) == "number" then Essence = v end end)
end)
refreshEssence()

local function updateAuraState(state) if type(state) == "table" then auraState = state end end
local function refreshAuraState()
    pcall(function()
        local res = AuraService:GetState()
        if type(res) == "table" and res.andThen then res:andThen(updateAuraState):catch(function() end)
        elseif type(res) == "table" then updateAuraState(res) end
    end)
end
if AuraService.StateUpdated then AuraService.StateUpdated:Connect(updateAuraState) end

local function updateCuttersState(state)
    if type(state) == "table" then
        if type(state.CuttersData) == "table" then cuttersData = state.CuttersData end
        if type(state.OwnedCutters) == "table" then ownedCutters = state.OwnedCutters end
        if type(state.CurrentCutter) == "string" then currentCutter = state.CurrentCutter end
    end
end
local function refreshCuttersState()
    pcall(function()
        CuttersShopService:GetShopState():andThen(updateCuttersState):catch(function() end)
    end)
end
if CuttersShopService.ShopInited then
    CuttersShopService.ShopInited:Connect(function(owned, data, equipped, robux)
        updateCuttersState({ OwnedCutters = owned, CuttersData = data, CurrentCutter = equipped, CutterRobuxPrices = robux })
    end)
end
if CuttersShopService.ShopUpdated then
    CuttersShopService.ShopUpdated:Connect(function(owned, equipped)
        if type(owned) == "table" then ownedCutters = owned end
        if type(equipped) == "string" then currentCutter = equipped end
    end)
end

UpgradesService.SpeedUpgradeUpdated:Connect(function(_, _, cost, max) upgradePrices["Speed"] = tonumber(cost) or math.huge; upgradeMaxed["Speed"] = max == true end)
UpgradesService.CarryUpgradeUpdated:Connect(function(_, _, cost, max) upgradePrices["Carry"] = tonumber(cost) or math.huge; upgradeMaxed["Carry"] = max == true end)
UpgradesService.AttackSpeedUpgradeUpdated:Connect(function(_, _, cost, max) upgradePrices["Attack Speed"] = tonumber(cost) or math.huge; upgradeMaxed["Attack Speed"] = max == true end)
UpgradesService.AttackRangeUpgradeUpdated:Connect(function(_, _, cost, max) upgradePrices["Attack Range"] = tonumber(cost) or math.huge; upgradeMaxed["Attack Range"] = max == true end)
pcall(function()
    UpgradesService:RequestStateSync():catch(function() end)
end)

DataService:GetData("Money"):andThen(function(v) if type(v) == "number" then money = v end end):catch(function() end)
DataService.UpdateMoney:Connect(function(v) if type(v) == "number" then money = v end end)

local function updateBackpackFull(state)
    if type(state) == "table" then
        local current = tonumber(state.BackpackLoad) or tonumber(state.Count) or tonumber(state.SlotCount) or 0
        local capacity = tonumber(state.Capacity) or 0
        backpackFull = capacity > 0 and current >= capacity
    end
end
local function refreshBackpackFull()
    pcall(function()
        DataService:GetBackpackSlotsState():andThen(updateBackpackFull):catch(function() end)
    end)
end
pcall(function()
    DataService.BackpackSlotsUpdated:Connect(updateBackpackFull)
end)
refreshBackpackFull()

task.spawn(function() while true do refreshEssence(); task.wait(2) end end)

local function craftRelic(relicName)
    if isCrafting then return end
    local id = relicIdByName[relicName]
    if not id then return end
    local cost = relicCostById[id]
    if Essence < cost then return end
    isCrafting = true
    pcall(function() StartCraftRemote:InvokeServer(id) end)
    task.wait(0.5)
    isCrafting = false
    refreshEssence()
end
task.spawn(function()
    while true do
        if autoCraftEnabled and not isCrafting then
            for _, name in ipairs(selectedRecipes) do
                if autoCraftEnabled and not isCrafting then craftRelic(name) end
            end
        end
        task.wait(0.5)
    end
end)

local function getRelicScore(relic)
    local tier = relic.Tier or 0
    local buffs = relic.Buffs or {}
    local score = tier * 1000000
    for _, v in pairs(buffs) do score = score + (tonumber(v) or 0) end
    return score
end

local function autoEquipBestRelic()
    if not autoEquipRelicEnabled or isEquippingRelic then return end
    isEquippingRelic = true
    pcall(function()
        RelicsService:GetState():andThen(function(state)
            if type(state) ~= "table" then
                isEquippingRelic = false
                return
            end

            local owned = state.OwnedRelics or {}
            local equipped = {}
            if type(state.EquippedRelicIds) == "table" then
                for _, id in ipairs(state.EquippedRelicIds) do
                    equipped[tonumber(id)] = true
                end
            end

            local ownedList = {}
            for id in pairs(owned) do
                local nid = tonumber(id)
                if nid and RelicsConfig.ItemsById[nid] then
                    table.insert(ownedList, nid)
                end
            end
            table.sort(ownedList, function(a, b)
                return getRelicScore(RelicsConfig.ItemsById[a]) > getRelicScore(RelicsConfig.ItemsById[b])
            end)

            local rebirthLevel = tonumber(LocalPlayer:GetAttribute("RebirthLevel")) or 0
            local maxSlots = 2
            if rebirthLevel >= 5 then maxSlots = maxSlots + 1 end
            if rebirthLevel >= 12 then maxSlots = maxSlots + 1 end
            if state.RobuxSlotUnlocked == true then maxSlots = maxSlots + 1 end

            local equippedCount = 0
            for _ in pairs(equipped) do equippedCount = equippedCount + 1 end

            local toEquip = {}
            for _, id in ipairs(ownedList) do
                if not equipped[id] then
                    if equippedCount < maxSlots then
                        table.insert(toEquip, id)
                        equippedCount = equippedCount + 1
                    end
                end
            end

            local function equipNext(index)
                if index > #toEquip then
                    isEquippingRelic = false
                    return
                end
                RelicsService:EquipRelic(toEquip[index]):andThen(function()
                    equipNext(index + 1)
                end):catch(function()
                    equipNext(index + 1)
                end)
            end

            equipNext(1)
        end):catch(function()
            isEquippingRelic = false
        end)
    end)
end
task.spawn(function()
    while true do
        autoEquipBestRelic()
        task.wait(1)
    end
end)

task.spawn(function()
    while true do
        if autoCollectCraftedEnabled then
            pcall(function() ClaimCraftResultRemote:InvokeServer() end)
        end
        task.wait(1)
    end
end)

local function isAuraWorldLocked(name)
    local req = AurasConfig.RequiredWorldByAuraId and AurasConfig.RequiredWorldByAuraId[name]
    if not req then return false end
    local highest = tonumber(LocalPlayer:GetAttribute("HighestVisitedWorld")) or 1
    return highest < math.max(1, tonumber(req))
end
local function isAuraOwned(name)
    if not auraState or not auraState.Items then return false end
    local item = auraState.Items[name]
    return item and item.Owned == true
end
local function isAuraEquipped(name)
    if not auraState or not auraState.Items then return false end
    local item = auraState.Items[name]
    return item and item.Equipped == true
end
local function findCheapestAffordableUnownedAura()
    local bestName, bestPrice = nil, math.huge
    for _, auraName in ipairs(auraNames) do
        if not isAuraOwned(auraName) and not isAuraWorldLocked(auraName) then
            local price = tonumber(AurasConfig.Items[auraName].Price) or 0
            if price <= money and price < bestPrice then
                bestName = auraName
                bestPrice = price
            end
        end
    end
    return bestName
end
local function findBestOwnedAura()
    local bestName, bestBoost = nil, -math.huge
    for auraName, data in pairs(AurasConfig.Items) do
        if isAuraOwned(auraName) then
            local boost = tonumber(data.StrengthMultiplier) or 0
            if boost > bestBoost then
                bestBoost = boost
                bestName = auraName
            end
        end
    end
    return bestName
end
local function buyOrToggleAura(auraName)
    if isBusyAura then return end
    isBusyAura = true
    pcall(function()
        local res = AuraService:BuyOrToggleAura(auraName)
        if type(res) == "table" and res.andThen then
            res:andThen(function(state)
                isBusyAura = false
                if type(state) == "table" and state.State then
                    updateAuraState(state.State)
                else
                    refreshAuraState()
                end
            end):catch(function()
                isBusyAura = false
                refreshAuraState()
            end)
        else
            isBusyAura = false
            refreshAuraState()
        end
    end)
end
task.spawn(function()
    while true do
        if autoBuyAuraEnabled then
            local name = findCheapestAffordableUnownedAura()
            if name then buyOrToggleAura(name) end
        end
        task.wait(0.5)
    end
end)
task.spawn(function()
    while true do
        if autoEquipAuraEnabled then
            local best = findBestOwnedAura()
            if best and not isAuraEquipped(best) then buyOrToggleAura(best) end
        end
        task.wait(0.1)
    end
end)

local function isCutterWorldUnlocked(name)
    local data = cuttersData[name]
    if not data then return false end
    local requiredWorld = tonumber(data.RequiredWorld) or 1
    local highestWorld = tonumber(LocalPlayer:GetAttribute("HighestVisitedWorld")) or 1
    return highestWorld >= requiredWorld
end
local function findCheapestAffordableUnownedCutter()
    local bestName, bestPrice = nil, math.huge
    for name, data in pairs(cuttersData) do
        if not ownedCutters[name] and isCutterWorldUnlocked(name) and not (data.HiddenUntilOwned == true) then
            local price = tonumber(data.Price) or 0
            if price <= money and price < bestPrice then
                bestName = name
                bestPrice = price
            end
        end
    end
    return bestName
end
local function findBestOwnedCutter()
    local bestName, bestDamage = nil, -math.huge
    for name, data in pairs(cuttersData) do
        if ownedCutters[name] then
            local dmg = tonumber(data.DamageMult) or 0
            if dmg > bestDamage then
                bestDamage = dmg
                bestName = name
            end
        end
    end
    return bestName
end
local function buyCutter(name)
    if isBuyingCutter then return end
    isBuyingCutter = true
    pcall(function()
        CuttersShopService:BuyCutter(name):andThen(function()
            isBuyingCutter = false
            refreshCuttersState()
        end):catch(function()
            isBuyingCutter = false
        end)
    end)
end
refreshCuttersState()
task.spawn(function()
    while true do
        if autoBuyCutterEnabled then
            local name = findCheapestAffordableUnownedCutter()
            if name then buyCutter(name) end
        end
        task.wait(0.5)
    end
end)
task.spawn(function()
    while true do
        if autoEquipCutterEnabled then
            local best = findBestOwnedCutter()
            if best and best ~= currentCutter then buyCutter(best) end
        end
        task.wait(0.1)
    end
end)

local function fireUpgrade(internalName)
    if internalName == "Speed" then
        UpgradesService.SpeedButtonClicked:Fire()
    elseif internalName == "Carry" then
        UpgradesService.CarryButtonClicked:Fire()
    elseif internalName == "Attack Speed" then
        UpgradesService.AttackSpeedButtonClicked:Fire()
    elseif internalName == "Attack Range" then
        UpgradesService.AttackRangeButtonClicked:Fire()
    end
end
task.spawn(function()
    while true do
        if autoUpgradesEnabled then
            for _, displayName in ipairs(selectedUpgrades) do
                local internal = displayToInternal[displayName]
                if internal and not upgradeMaxed[internal] and money >= upgradePrices[internal] then
                    fireUpgrade(internal)
                    task.wait(0.1)
                end
            end
        end
        task.wait(0.1)
    end
end)

local function getLootPriority(rarity)
    for i, r in ipairs(rarityOrder) do
        if r == rarity then return i end
    end
    return #rarityOrder + 1
end
local function getLootPosition(loot)
    if loot:IsA("Model") then
        return loot:GetPivot().Position
    elseif loot:IsA("BasePart") then
        return loot.Position
    else
        return loot:GetPivot().Position
    end
end
local function findPrompt(loot)
    local direct = loot:FindFirstChild("PickupPrompt")
    if direct and direct:IsA("ProximityPrompt") then return direct end
    for _, child in ipairs(loot:GetDescendants()) do
        if child.Name == "PickupPrompt" and child:IsA("ProximityPrompt") then return child end
    end
    return nil
end
local function triggerPrompt(prompt)
    if fireproximityprompt then
        fireproximityprompt(prompt)
    elseif prompt.InputHoldBegin then
        prompt:InputHoldBegin()
        task.wait(0.1)
        prompt:InputHoldEnd()
    else
        pcall(function()
            game:GetService("ProximityPromptService"):Prompt(prompt)
        end)
    end
end
local function isLootValid(loot)
    return loot and loot.Parent and CollectionService:HasTag(loot, "SpawnedLoot")
end
local function getBestLoot()
    local lootList = {}
    for _, loot in ipairs(CollectionService:GetTagged("SpawnedLoot")) do
        if loot and loot.Parent then
            local rarity = loot:GetAttribute("LootRarity")
            if rarity and selectedLootRarityMap[rarity] then
                table.insert(lootList, loot)
            end
        end
    end
    if #lootList == 0 then return nil end
    table.sort(lootList, function(a, b)
        local pa = getLootPriority(a:GetAttribute("LootRarity"))
        local pb = getLootPriority(b:GetAttribute("LootRarity"))
        if pa ~= pb then return pa < pb end
        local hrp = getHRP()
        if hrp then
            return (getLootPosition(a) - hrp.Position).Magnitude < (getLootPosition(b) - hrp.Position).Magnitude
        end
        return false
    end)
    return lootList[1]
end
task.spawn(function()
    while true do
        if autoCollectLootEnabled then
            refreshBackpackFull()
            if backpackFull then
                returnToSavedPosition()
                while autoCollectLootEnabled and backpackFull do
                    task.wait(1)
                    refreshBackpackFull()
                end
                if not autoCollectLootEnabled then break end
            end
            local target = getBestLoot()
            if target then
                while target and isLootValid(target) and autoCollectLootEnabled do
                    local better = getBestLoot()
                    if better and better ~= target and getLootPriority(better:GetAttribute("LootRarity")) < getLootPriority(target:GetAttribute("LootRarity")) then
                        target = better
                    end
                    if backpackFull then
                        returnToSavedPosition()
                        while autoCollectLootEnabled and backpackFull do
                            task.wait(1)
                            refreshBackpackFull()
                        end
                        if not autoCollectLootEnabled then break end
                        target = getBestLoot()
                        if not target then break end
                    end
                    local pos = getLootPosition(target)
                    local hrp = getHRP()
                    if not hrp then
                        task.wait(0.2)
                        break
                    end
                    hrp.CFrame = CFrame.new(pos + Vector3.new(0, 2, 0))
                    task.wait(0.2)
                    local prompt = findPrompt(target)
                    if prompt then triggerPrompt(prompt) end
                    task.wait(0.2)
                    if not isLootValid(target) then break end
                end
            else
                task.wait(0.2)
            end
        else
            task.wait(0.2)
        end
    end
end)

task.spawn(function()
    while true do
        if autoClickEnabled then
            pcall(function()
                ClickRequestedRemote:FireServer()
            end)
            task.wait(0.01)
        else
            task.wait(0.1)
        end
    end
end)

task.spawn(function()
    while true do
        if autoRebirthEnabled then
            if rebirthCooldown > 0 then
                rebirthCooldown = rebirthCooldown - 0.1
                task.wait(0.1)
                continue
            end
            pcall(function()
                RebirtService:GetState():andThen(function(state)
                    if type(state) == "table" and state.CanRebirth == true and state.IsMaxRebirth ~= true then
                        RebirtService.RebirthButtonClicked:Fire()
                        rebirthCooldown = 3
                    end
                end):catch(function() end)
            end)
        end
        task.wait(0.1)
    end
end)
